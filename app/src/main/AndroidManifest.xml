<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.merann.smamonov.googledrive">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.SettingsActivity" />
        <activity android:name=".view.OpenFileActivity" />

        <service android:name=".service.DriveService">
            <intent-filter>
                <action android:name="com.merann.smamonov.googledrive.DriveService" />
            </intent-filter>
        </service>
        <service android:name=".service.ConfigurationService">
            <intent-filter>
                <action android:name="com.merann.smamonov.googledrive.ConfigurationService" />
            </intent-filter>
        </service>


    </application>

</manifest>
